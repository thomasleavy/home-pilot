# ============== MQTT broker (backend + simulator) ==============
MQTT_BROKER_URL=localhost
MQTT_BROKER_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# ============== Backend ==============
PORT=3000
API_KEY=
JWT_SECRET=change-this-to-a-random-secret-in-production
# Optional: SQLite DB path (default: backend/data/app.db)
# SQLITE_PATH=./data/app.db
# Optional: set to 1 to log heating/hot-water commands and history (for debugging Insights)
# INSIGHTS_DEBUG=1

# ============== Frontend ==============
# API URL is set in frontend/src/environments/environment.ts (and environment.prod.ts).
# Change apiUrl there if your backend runs on a different host/port.
